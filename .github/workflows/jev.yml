# Generated by jev-elder/core/sync.mjs. Edit the tests command there, then re-sync.
name: jev
on:
  push:
  pull_request:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: dtolnay/rust-toolchain@stable
      - run: npm install --no-audit --no-fund
      - run: node .jev/check.mjs --tests "npm test && cargo test --manifest-path crates/jev-kernel/Cargo.toml"
